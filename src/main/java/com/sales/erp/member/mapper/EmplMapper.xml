<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
     "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sales.erp.member.dao.EmplMapper">
	<insert id="Buy_Request_Action" parameterType="com.sales.erp.member.vo.OrderRequestVO">
		INSERT INTO
		OREQUEST
		(ONUM, EMPNO, PROCODE, OAMOUNT, OCOMMENT, REGDATE, CHECKS,
		DELETED)
		VALUES(TO_CHAR(ONUM_SEQ.NEXTVAL), #{empno}, #{procode},
		#{oamount}, #{ocomment}, SYSDATE, 0, 0)
	</insert>

	<insert id="Buy_Request_Action_re" parameterType="com.sales.erp.member.vo.OrderRequestVO">
		INSERT INTO
		OREQUEST
		(ONUM, EMPNO, PROCODE, OAMOUNT, OCOMMENT, REGDATE, CHECKS,
		DELETED)
		VALUES((SELECT MAX(ONUM) FROM OREQUEST), #{empno}, #{procode},
		#{oamount}, #{ocomment}, SYSDATE, 0, 0)
	</insert>

	<select id="getRequestList" resultType="com.sales.erp.member.vo.OrderRequestVO">
		SELECT * FROM OREQUEST WHERE EMPNO=#{empno}
	</select>
</mapper>
